using UnityEngine;

public class UltStart : MonoBehaviour
{
    public GameObject ult;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void Act()
    {
        ult.SetActive(true);
    }
}
